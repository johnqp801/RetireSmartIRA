# HANDOFF — V2.1.1: finish Alan's fixes + submit to Apple (2026-07-13)

**Read this first next session.** Everything is committed; nothing is lost. This picks up mid-flight.

## Goal for the next session
1. Finish Alan Levy's remaining feedback fixes (#2, #4, #5) on the `2.1.1/user-feedback-fixes` branch.
2. Merge that branch → `main`, re-tag `v2.1.1-build61` at the true-final commit.
3. John archives **2.1.1 / build 61** (Any Mac + Any iOS Device) and submits to the App Store.

## Git state (verify with `git worktree list`, `git log`, `git tag`)
- **`main`** = **2.1.1 / build 61** with the Multi-Year engine fixes already merged + reviewed (B4, A3, A4, B2, A5, A2). Final whole-branch review = READY. Live in App Store is still 2.1.0/60 (2.1.1 NOT submitted yet).
- **`2.1.1/user-feedback-fixes`** (worktree `.worktrees/2.1.1-user-feedback`, off main) = Alan's fixes in progress. **NOT merged.** Has #1 (keyboard) + #3 (heat-map) done + committed. Plan: `docs/superpowers/plans/2026-07-13-v2.1.1-alan-feedback-fixes.md`.
- **Tag:** the premature `v2.1.1-build61` tag was DELETED (it pointed at code missing the Alan fixes). Re-create it at the true-final commit AFTER all Alan fixes merge to main.
- SDD ledger (gitignored, may not persist): `.superpowers/sdd/progress.md` on the user-feedback branch. This handoff supersedes it.

## What's DONE in 2.1.1
**Multi-Year (on main, all reviewed clean):** B4 phantom conversions (cap both optimizer paths + `executedRothConversion` + 6 consumers routed), A3 gross-up into IRMAA+NIIT MAGI, A4 tax-funding withdrawal disclosed, B2 comparison shows PV lifetime tax, A5 optimizer keep-best (recommendedTaxMin returns min-objective over greedy + deterministic ladders, gated to non-convergent for perf), A2 fill-to-bracket "ordinary income" clarity caption. Suite ~1848 green.
**Alan (on user-feedback branch):**
- **#1 keyboard trap (SEVERE):** reusable `.dismissableKeyboard()` (iOS-gated, macOS no-op) in `KeyboardDismiss.swift`, applied to sheets (AddAccount/AddIncome/SSDataEntry) + tab roots + DIRECTLY on `SettingsView`(My Profile) form. Reviewed APPROVED. **OPEN: needs an on-device tap-test of Profile → birth-year keyboard before ship** (reviewer's one caveat; the `KeyboardDismissTests` are compile-only smoke tests).
- **#3 gray 9x9 heat-map:** pure `SSCouplesMatrixColor` 3-color scale normalized across the matrix min/max, applied to cells. Focused tests green. **Salvaged after the implementer agent stalled — NOT formally task-reviewed; the final whole-branch review must cover it.**

## What's REMAINING (do these next, on the user-feedback branch, via SDD)
Plan Tasks 3-6 + gate. Briefs: `scripts/task-brief docs/.../2026-07-13-v2.1.1-alan-feedback-fixes.md N`.
- **#2 (plan Task 3): blank "best claiming age for [spouse]" table** in `SSCouplesStrategyView.swift` (~lines 88-96, 111). Reproduce with one-spouse-claimed; the filtered matrix goes empty (filter key mismatch `primaryClaimingAge`/`spouseClaimingAge`, claimed age out of matrix range, or spouse SS `hasData==false`). Fix + show an empty-state message instead of blank.
- **#4 (plan Task 4): cumulative-benefits colors too similar** in `SSChartsView.swift` `colorScale` (~46-58). CONFIRMED cause: single-hue teal ramp WITH collisions ("Claim at 67"&"68" both tealRamp5; "69"&"70" both tealRamp6). Replace with a differentiated categorical palette; each displayed claim age distinct; theme-aware.
- **#5a (plan Task 5): SS withholding → IRS W-4V rate picker.** RESEARCHED: SS federal withholding is ONLY 7/10/12/22% (or none), NO dollar amounts allowed (IRS Form W-4V, https://www.irs.gov/pub/irs-pdf/fw4v.pdf). Replace the dollar "Annual Federal Withholding" for SS with a picker None/7/10/12/22%; withheld $ = rate × SS benefit fed to the tax/safe-harbor calc. **Back-compat: existing profiles have a legacy SS dollar value — migrate to nearest legal rate or default None with a one-time note; DO NOT zero silently.** Engine consumes DOLLARS — resolve % to $ at the consumption point; don't change engine semantics.
- **#5b (plan Task 6): other-income $/% withholding toggle** (pension, IRA/401k, etc.) in `IncomeSourcesView.swift` + income model. Per-source mode (.dollars/.percent) + value; legacy dollar values default .dollars (byte-identical). CAN be deferred without blocking #2/#4/#5a if it balloons (persistence migration).
- **Gate (plan Task 7):** full suite green + both-platform build; review the (only #5-caused) rebaselines.

## To SHIP 2.1.1 (after the above)
1. Final whole-branch review of the user-feedback branch (must cover #3, which was salvaged un-reviewed).
2. Merge `2.1.1/user-feedback-fixes` → main (ff). Re-tag `v2.1.1-build61`.
3. **Release notes:** 3 options were presented (John has NOT picked): A=refinements-bulleted, B=concise-paragraph, C=accuracy-led-bulleted. Full text in the previous turn / `drafts/marketing/2026-07-12-2.1.0-app-store-metadata.md` predecessor. **Description UNCHANGED from 2.1.0** (2.1.1 is refinements). Save the picked wording to `drafts/marketing/` per house rule.
4. Version stays 2.1.1 / build 61 (nothing uploaded yet, so no re-bump). John archives + uploads + submits.

## Environment note
This handoff exists partly because the machine got heavily loaded late in a marathon session: 2-min build timeouts, one API ConnectionRefused killing an agent, and full-suite runs so slow the implementer subagents stalled waiting on them (had to hand-finalize #1 and #3). A fresh session on a rested machine will go smoother — especially for #5.

## NEXT CYCLE (NOT in 2.1.1 — deferred, see fix-backlog)
A1 (objective over-converts → after-tax-wealth reframe; John chose to defer, decisions log 2026-07-13); productionize the invariant sweep as asserting `MultiYearInvariantTests`; expand the oracle set; C5/C6 chart polish (IRMAA-line clutter, nominal-default). Full list: `.claude/memory/roadmap/2026-07-13-multi-year-fix-backlog.md`.
