# 2026-07-17 — Marathon: 2.1.2 bundle built+merged, live audit, data-loss forensics

One very long session. Everything below is on `main` (local, UNPUSHED past `295240e`); suite green at every merge (final: 1,403 Swift Testing + 503 XCTest).

## 1. Morning/afternoon — engine + feature work (merged as `3b69e87`)
- **Branch cleanup:** stale `2.0/heir-tax-optimizer-objective` deleted after rescuing 6 stranded memory files to main (`295240e`); root worktree now on `root-workspace`; John pushed main to origin at that point (origin later fell behind again — see Open).
- **Cross-λ Pareto repair** (`25f47f5`) for the harness-caught dominated-frontier bug.
- **Wealth-consistent objective** (`900ba6d`): ALL tax flows discount at the growth rate; λ=0 over-conversion cured at the root; frontier genuinely opens; `brakeStopsDrain` re-enabled; `pvRealDiscountRate` truly display-only.
- **Widow banner** (`7a08419`): nominal in-horizon tax paid (new `OptimizationEngine.nominalLifetimeTax`), resolving its §8-5 units question.
- **Harness branch** `2.2/display-audit-harness` rebased onto the fixes; gate rebaselined to EMPTY offender set (`931f4b6`). NOTE: needs ANOTHER rebase now (perYearOverrides rename + tonight's merges).
- **2.1.2 scope decision** (see decisions log): B4/A4 verified ALREADY SHIPPED in 2.1.1; built Alan #2 state-withholding % (`27e738c`), Alan #1 local/city income tax (`1290a20`).
- **Per-year expense overrides** designed (brainstorm → spec → plan, all committed) and built via subagent-driven development (7 tasks + final-review fix wave `4a1ee2b`): recurring-anchor + one-time model, behavior-preserving legacy→delta migration (idempotent/atomic), YearDetailEditor, badges. Final whole-branch review caught 2 cross-task bugs (stale optimal on override save; legacy-map erasure window) — both fixed.
- All merged to main as **`3b69e87`**; feature branch + worktree removed.

## 2. Evening — Alan's email + live audit of John's hand-built test household
- Alan's 5:35 AM email: local tax + state-% (both now built) + "numbers for 2027+" (expenses slice built; income = v2.2).
- John built a test household live in his **2.1.1 App Store build** (MFJ b.1955 FL, $8.24M trad, $1.1M Roth, taxable "B", $300k expenses, SS receiving $87k/yr): "why no Roth conversion in 2027?" → answered (income spike year); wrote a ChatGPT cross-review brief; ChatGPT's analysis reconciled:
  - **ChatGPT right:** nominal-vs-PV mixing (the +$4.4M heir headline is +$1.0M PV — confirmed via toggle).
  - **ChatGPT wrong on substance:** bumping terminal rate 22%→35% makes the engine convert MORE (heirs $4.8M→$5.5M PV) — the $4.9M plan is robust, not an artifact of the low rate.
  - Discovered live: **Advanced-sheet edits don't recompute** (Done fires nothing; only approach toggles do) — even on current main.

## 3. Late night — three trust fixes (merged as `d607e59`)
1. **PV default** (B5+C6): Multi-Year defaults to Present value; CPA export lifetime-tax rows in labeled PV; `MultiYearPlanView.defaultUnits` pinned by test.
2. **Value-based recompute**: `MultiYearStrategyManager.engineRelevantChanged(old,new)` + `.onChange(of: manager.assumptions)` — the architectural fix for the invalidation class (5th instance today); UI-only fields excluded.
3. **SS data-loss guard**: `saveSSData` never removes stored keys on nil memory (was: one failed load + any auto-save = permanent erasure). Injectable defaults; `SSPersistenceSafetyTests`.

## 4. DATA-LOSS EVENT (resolved understanding; John must re-enter)
- John entered taxable **B = $6,200,000** and the **SS benefits** TONIGHT in his 2.1.1 instance. Disk forensics (container plist decoded): B stored at **$200k** (his earlier entry), **no SS keys at all**; IRAs + income sources fully intact. Host plist is an April-era 446-byte shell; single container; no TM snapshots.
- Tonight's dev builds did NOT corrupt anything — they showed disk truth. The $6.2M + SS lived only in 2.1.1's memory and were lost when that instance quit (surprising given 2.1.1's save hooks — exact sequence unrecoverable without logs; the remove-on-nil pattern (now fixed) is the standing destroyer that makes any such failure permanent).
- Plist backup of current state: scratchpad `prefs-backup-215108.plist`.
- **JOHN TO RE-ENTER (in a current-main build): taxable B → $6,200,000; SS benefits (You $5,400/mo claimed@70; Spouse $1,850/mo claimed@67; COLA 2.8%).**

## Open items / next session
- **Re-enter lost data** (above) then LIVE-verify the recompute fix (terminal-rate change → Done → table updates without approach toggle) — the merged-main app is built at DerivedData `…dugydacodetsgffebiubqmogpbjk` (launched at session end).
- **Push main** (many commits local-only: `3b69e87`, `d607e59`, memory commits).
- **Harness branch** needs rebase onto new main (AuditProfiles uses old `perYearExpenseOverrides:` label — compile break) + packet baselines shift under the new objective.
- **Remove-on-nil audit**: PersistenceManager has similar removeObject-on-nil sites (deductionOverride, legacy heir, Medicare overrides) — lower risk (nil is a real user state there) but worth an explicit pass.
- **2.1.2 release** still deliberately not cut: version bump, release notes (2-3 options), whole-bundle verification, tester pass. Alan reply still to draft. Fred vision email still for John to send.
- Backlog seeds: units enum misnomer (`.todaysDollars` = nominal "Future $"); discoverability nudge for "the tab already projects future years" (explicitly out of 2.1.2's expense slice).
