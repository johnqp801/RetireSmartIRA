# Session 2026-07-13 — 2.1.0 ship (2.1.1 folded in) + deep Multi-Year audit

Branch: `2.1/selectable-conversion-approaches` (worktree). Long session; two arcs.

## Arc 1 — Build & ship 2.1.0 (2.1.1 combined in)
Decision (John): **fold "2.1.1" cash-charitable itemizing into the 2.1.0 release** (one release, not a separate ship). Built via SDD (brainstorm → spec → plan → 9 tasks, each implemented + reviewed clean):
- Per-year standard-vs-itemized selection in the multi-year `ProjectionEngine`; new pure `MultiYearItemizedDeduction` helper (SALT cap+phaseout, medical floor, charitable 60% ceiling + OBBBA 0.5% floor, §68, §170(p)) byte-matches single-year `DataManager` (parity-tested); itemizables carried from single-year; cash-only, no carryforward/AMT. Commits d331e55..b30338c. Full suite **1819 green**.
- Spec `docs/superpowers/specs/2026-07-12-multiyear-cash-charitable-itemizing-design.md`, plan `docs/superpowers/plans/2026-07-12-multiyear-cash-charitable-itemizing.md`, SDD ledger `.superpowers/sdd/progress.md` (PHASE 2.1.1).
- **Version bumped 2.0.1/59 → 2.1.0/60** both platforms (commit 1bebdf8). Live in App Store was 2.0.1 build 59.
- Marketing: `drafts/marketing/2026-07-12-2.1.0-app-store-metadata.md` — Option-A feature-led release notes + 3,993-char description (only the Multi-Year Plan paragraph changed from 2.0.0). Promotional text options offered (John leaning D, not finalized in-file).
- **John submitted 2.1.0 to the App Store 2026-07-12.** Remaining: merge branch→main + tag `v2.1.0-build60` once Apple-approved (standing rule: no merge until shipped). See [[v2-1-engine-status]].
- Fred Lucha email drafted (submitted-v2.1 + review-the-Multi-Year-graphs/CPA-report ask): `drafts/emails/2026-07-12-fred-lucha-v2.1.0-submitted-multiyear-review-ask.md`. FINAL, unsent, from john@retiresmartira.com.

## Arc 2 — Over-conversion root cause + Multi-Year audit (John walking the tabs)
- **Over-conversion (A1) ROOT-CAUSED:** "Minimize lifetime tax" minimizes PV(in-horizon tax)+PV(0.22×terminal trad), NOT after-tax wealth; drains IRA into 32–37% (wealth-suboptimal vs a gentle fill by ~$114k). Decision: ship 2.1.0 anyway (pre-existing, ~2% wealth impact); fix objective next cycle. See [[over-conversion-brake-ineffective]].
- **SS $115k taxable "bug" = John's input error** (muni interest also entered as a Social Security source). Engine correct.
- **Multi-Year graphs "$150M" = inputs + nominal display**, not an engine bug (growth applied once; sensible defaults). Fixed by user setting realistic account price-growth.
- **NEW living fix-list: `roadmap/2026-07-13-multi-year-fix-backlog.md`** — the durable output. Items: A1 (objective over-conversion), A2 (fill-to-22 is correct; AGI-clarity — traced, NOT a bug), A3 (gross-up doesn't recompute IRMAA/ACA — real calc gap), A4 (gross-up outflow not disclosed), B2 (comparison shows nominal lifetime tax while optimizer minimizes PV → "minimize" looks higher), B4 (phantom $500k conversions after IRA drains + downstream reporting audit), C5/C6 (chart clutter/nominal default), D/E cleanup+seams, F guardrail, G external-review reconciliation.
- **External AI review reconciled** (`review-external` skill, code-cited): its principles all correct, but the ENGINE already handles classification (ordinary vs preferential investment income), NIIT (`min(NII, MAGI−threshold)`), and incremental attribution (`ConsequenceDeltas = selected − baseline`) correctly. Two real items survived: A3 (gross-up IRMAA) + A1. Everything else = disclosure. **Lesson: my live verbal explanations were looser than the engine — verify before characterizing.**
- **B4 REPRODUCED + downstream audit (John's ask):** `runDeterministicLadder` `upperBoundCap = min(startingBalances.traditional, 500k)` uses the STARTING balance, so it requests $500k every year even after the IRA drains (~2034); projection clamps to $0 but the display shows the request. **Downstream audit:** tax/balances/IRMAA/NIIT/legacy read the CLAMPED projection = CORRECT; but 6 sites read requested `rec.actions` and inflate conversion-AMOUNT reporting: PlanSummary.totalConversions ("$11.6M"), PlanPathMetrics.peakAnnualRothConversion (comparison "peak conversion"), ConversionLadderChart, LadderRow, MultiYearCPABriefing (the CPA export → Fred), StrategySummarySynthesizer. Fix: upperBoundCap→current-year balance + add `executedRothConversion` to YearRecommendation, route all 6 to it.

## Open / next
- Merge 2.1.0 → main + tag once Apple-approved.
- **Before sending Fred the CPA export**, note the conversion figures are inflated (B4) — consider fixing B4 first.
- Objective fix (A1) + B4 fix + disclosure items = next-cycle Multi-Year work; see the fix-backlog.
- Promotional text: John leaning option D ("Pick your Roth conversion strategy…"); finalize + save.
