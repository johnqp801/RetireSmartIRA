# Session 2026-07-10 — V2.1 selectable conversion approaches: spec → entire engine built

One very long session: from Fred Lucha's feature request through a frozen spec (three external-review reconciliations) to the **complete engine** for V2.1.0's selectable Roth-conversion feature — five phases, all executed via subagent-driven development with adversarial reviews. **Nothing merged to `main`.**

## Arc
1. **Trigger:** Fred Lucha emailed asking to model "convert the most without triggering IRMAA / while staying in 22%," and observed the optimizer drains the whole traditional IRA pre-RMD. This is exactly the selectable-objective gap ([[optimizer-objective-not-selectable]]) also promised to Tim. (John did NOT want to reply to Fred yet.)
2. **Brainstorm + competitive research** (`reference/2026-07-10-competitive-analysis-conversion-tools-and-qcd.md`): selectable bracket/IRMAA goals + correct-AGI QCD are table stakes (Boldin/Pralana/RightCapital/ProjectionLab); reversed the 2026-07-08 "comparison-view-only" decision.
3. **Frozen spec:** `docs/superpowers/specs/2026-07-10-selectable-conversion-approaches-charitable-modeling-design.md`. Reconciled against ChatGPT ×2 + Perplexity reviews + a 5-thread source-code verification (`reference/2026-07-10-v2.1-spec-reconciliation-findings.md` — the single best resume doc, with all verified engine facts + every Phase's landing notes + follow-ups).

## Branch state — `2.1/selectable-conversion-approaches` (worktree `.worktrees/2.1-selectable-conversion-approaches/`, off `main` @ fa6197f)
19 engine commits, **full suite 1,737 green, 0 unexplained rebaselines, NOT merged to main.** Five phases, each with its own plan in `docs/superpowers/plans/` and a whole-branch opus review (all "Ready = Yes"):
- **Phase 0 — NIIT** wired into the multi-year `ProjectionEngine` (closed a shipped gap; own `niit` channel).
- **Phase 1a — IRA/401k split** (`TradBucket`, 401k-first debit preserves IRA for QCDs; behavior-preserving).
- **Phase 1b — giving inputs** (`GivingIntent`/`QCDFundingMethod`/`CharitableGivingPlan` on `MultiYearStaticInputs`, adapter-seeded).
- **Phase 1c — recurring QCD application** (IRA-only via `debitIRA`, satisfies RMD, excluded from AGI, per-spouse, month-precise 70½ via threaded `birthDate` + `QCDPlanner`).
- **Phase 2a — selectable approaches** (`optimize(approach:)`: `.recommendedTaxMin` byte-identical default / `.fillToBracket(rate)` root-finds ordinary income to the bracket top / `.limitToIRMAA(tier,buffer)` caps MAGI every year; pure `ConversionLadder` bisection; new `YearRecommendation.taxablePreferential`/`.magi`).

## Key decisions (also in decisions/log.md 2026-07-10 + the reference file)
Co-equal selectable approaches (reverses 2026-07-08); minimum-core 2.1.0 vs deferred 2.1.1 (full itemized, full finite-diff decomposition, carryforward ledger); NIIT as a foundational fix; cash-charitable-not-deducted disclosed (2.1.0 QCD-only); precise month-level 70½ (birthDate exists in Profile, adapter had dropped the month); limit-to-IRMAA caps MAGI as a consistent income ceiling every year.

## REMAINING V2.1.0 work (not started)
- **Phase 2b — the comparison** (engine/model, testable via the loop): run the N approaches side by side (reuse `HeirFrontierCoordinator`'s "optimize N times" pattern) + dollar consequence flags (path-level deltas across federal/state/IRMAA/ACA/NIIT — cheap, both comparison paths already computed).
- **Phase 2c — SwiftUI**: "Conversion approach" picker + QCD/charitable inputs + the three-way comparison view in the Multi-Year tab (different testing modality — preview/visual, not the engine loop; wants a design pass).
- Then: version bump + App Store release workflow (both platforms), and eventually reply to Fred/Tim once shipped.

## Tracked follow-ups (non-blocking)
- **Widow-giving** (`task_88961daa`): `WidowStressTest.makeWidowVariant` drops income-composition fields + defaults giving plan `.none`/birthDate Jan-1-1970 → QCD inert there. When built, thread giving plan AND birthDate together.
- **Phase-2 display:** derive per-year conversion from the projected `YearRecommendation`, NOT the ladder's `locked` action amounts (can read slightly high when upperBoundCap > actual convertible).
- Add MFJ end-to-end tests for QCD (1c) and limit-to-IRMAA (2a); the gross-up fill-to-bracket landing edge; the cash-remainder Phase-2-UI disclosure.

## Process notes
Subagent-driven-development loop (fresh implementer + adversarial reviewer per task, whole-branch opus review per phase) caught real defects the implementers/reviewers found: a `taxablePreferential` gross-up basis mismatch, positional IRMAA-tier indexing, a no-double-debit QCD invariant, dropped inherited balances, invalid test scenarios. SDD ledger: `.superpowers/sdd/progress.md` (git-ignored scratch — the durable record is git + the reference file). Env note: `xcodebuild` stdout undercounts large Swift-Testing suites — use `-resultBundlePath` + `xcresulttool` for authoritative counts.

See [[optimizer-objective-not-selectable]], [[v2-status]].
