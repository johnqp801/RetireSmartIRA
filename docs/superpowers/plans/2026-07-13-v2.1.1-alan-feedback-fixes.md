# V2.1.1 — Alan Levy feedback fixes (fold into 2.1.1, not yet shipped)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Fix the five issues new user Alan Levy reported (keyboard trap, blank couples table, gray heat-map, low-contrast benefit colors, withholding input model) and fold them into the not-yet-submitted 2.1.1 (build 61).

**Architecture:** All five are single-year / Social-Security-planner / Profile / income-entry — separate from the Multi-Year engine already in 2.1.1. #1 is a global SwiftUI keyboard-dismiss gap; #2/#3 are SS-couples view bugs; #4 is a chart palette; #5 is a withholding INPUT-MODEL change (SS = IRS W-4V rate picker; other income = $/% toggle) that must still feed the tax/safe-harbor calc in DOLLARS.

**Tech Stack:** Swift, SwiftUI, Swift Testing, Xcode (macOS + iOS). Branch `2.1.1/user-feedback-fixes` off `main` (2.1.1 / build 61).

## Global Constraints
- Live app is 2.1.0/60; 2.1.1/61 not yet submitted, so these ride in build 61 (re-tag `v2.1.1-build61` at the true-final commit after merge).
- House rules: NO em dash (—) in any user-facing string; NO "Recommended" anywhere.
- **#5 domain fact (IRS Form W-4V):** Social Security federal withholding may ONLY be 7%, 10%, 12%, or 22% (or none) — flat dollar amounts are NOT permitted for SS. Source: https://www.irs.gov/pub/irs-pdf/fw4v.pdf
- The tax engine / safe-harbor / quarterly calc consumes withholding as DOLLARS — any %-based input must resolve to a dollar figure at the consumption point; do NOT change the engine's dollar semantics, only the input model + the $ it produces.
- Back-compat: existing saved profiles have dollar withholding values — migrate/prefill sensibly, never crash-decode or silently zero a user's data.
- Federal-only engine tests pin `"TX"`. Run with `-resultBundlePath` + `xcrun xcresulttool get test-results summary`. Full suite baseline = 2.1.1 (~1846). Commit with explicit pathspec.
- Verify on BOTH platforms where UI (keyboard behavior differs iOS vs macOS): the keyboard-dismiss fix (#1) is an iOS concern; guard it so macOS is unaffected.

---

### Task 1 (#1): Global keyboard dismissal for numeric fields — SEVERE
**Files:** likely a new `RetireSmartIRA/KeyboardDismiss.swift` (a reusable `ViewModifier` / toolbar), applied to the Profile view (child/spouse birth-year fields) and other numeric-entry screens. Grep the two views that already handle it (`SSDataEntryView.swift`, `TaxPlanningView.swift`) and reuse their pattern. Test: `RetireSmartIRATests/KeyboardDismissTests.swift` (pure-logic where possible; UI verified by build + manual note).

**Bug:** iOS number/decimal pads have no return key; only 2 views have any dismiss handling, so numeric fields (e.g. Profile child birth year) trap the keyboard and the user must force-quit.

- [ ] **Step 1:** Read the existing dismiss pattern in `SSDataEntryView.swift` + `TaxPlanningView.swift` (`@FocusState` + `.toolbar { ToolbarItemGroup(placement: .keyboard) { Spacer(); Button("Done"){...} } }`, or `.scrollDismissesKeyboard`). Extract a reusable modifier (e.g. `.dismissableKeyboard()`), iOS-gated (`#if os(iOS)`), no-op on macOS.
- [ ] **Step 2:** Apply it to the Profile view's numeric fields (find the profile view: grep `"My Profile"` / birth-year fields) and any other numeric-entry screens lacking dismissal. At minimum Profile + income entry.
- [ ] **Step 3:** Clean build BOTH platforms (`xcodebuild build ... -destination 'platform=macOS'` and `'generic/platform=iOS'` if available; else macOS + note iOS). Confirm no macOS regression.
- [ ] **Step 4:** Commit. (Note: full manual keyboard verification is device-only; document the code path + that a "Done" button now appears above numeric keyboards on iOS.)

### Task 2 (#3): 9x9 couples heat-map renders all one gray
**Files:** `RetireSmartIRA/SSCouplesStrategyView.swift` (the `matrixCard` + per-cell color), and wherever the cell color is computed (grep `SSCouplesMatrixCell`, `combinedLifetimeBenefit`, cell background). Test: extend/add SS couples view-logic test.

**Bug:** heat-map cells should show a 3-color scale by lifetime benefit; all render the same gray (color scale not firing).

- [ ] **Step 1:** Find the per-cell color logic. Determine why it collapses to gray — likely the value→color mapping returns a default, or the min/max normalization is degenerate (all cells map to one bucket), or a Color token is misresolved.
- [ ] **Step 2:** Write a failing pure-logic test on the cell-color function: given a matrix with a spread of `combinedLifetimeBenefit`, assert the best/worst/mid cells get DISTINCT colors (3 buckets). RED today.
- [ ] **Step 3:** Fix the color mapping (normalize across the matrix's actual min/max; map to the 3-color scale). GREEN.
- [ ] **Step 4:** Clean build; commit.

### Task 3 (#2): Blank "best claiming age for [spouse]" options table
**Files:** `RetireSmartIRA/SSCouplesStrategyView.swift` (the filtered-matrix table for the still-to-claim spouse, ~lines 88-96, 111). Test: SS couples view-logic test.

**Bug:** when displaying best claiming age for the wife, the claiming-options table is blank.

- [ ] **Step 1:** Reproduce: build inputs where one spouse has claimed and the other hasn't, and the "wife" is the still-to-decide spouse. Determine why the filtered matrix is empty — filter key mismatch (`primaryClaimingAge` vs `spouseClaimingAge`), the claimed spouse's locked age not in the matrix age range, or spouse SS benefit `hasData == false`.
- [ ] **Step 2:** Failing test: assert the options list for the still-to-claim spouse is non-empty for a valid two-spouse input; and if genuinely empty (missing spouse SS data), the view shows an explanatory empty-state, not a blank. RED.
- [ ] **Step 3:** Fix the filter/empty-state. GREEN.
- [ ] **Step 4:** Clean build; commit.

### Task 4 (#4): Cumulative-benefits claim-age colors too similar / colliding
**Files:** `RetireSmartIRA/SSChartsView.swift` (`colorScale`, ~lines 46-58). Test: color-distinctness unit test.

**Bug:** confirmed — the scale is a single-hue teal ramp AND collides: "Claim at 67" & "68" both `tealRamp5`; "69" & "70" both `tealRamp6`. Adjacent ages are indistinguishable.

- [ ] **Step 1:** Failing test: assert every DISTINCT displayed claim-age label maps to a DISTINCT color, and that adjacent ages (67 vs 70, the ones Alan compared) are visually distinguishable (different color tokens). RED today (collisions).
- [ ] **Step 2:** Replace the monochrome teal ramp with a differentiated categorical palette (reuse the app's chart palette tokens / dataviz system) so each claim age is clearly distinct; keep it theme-aware (light/dark). No em dash in any legend string.
- [ ] **Step 3:** Clean build; commit.

### Task 5 (#5a): Social Security withholding → IRS W-4V rate picker
**Files:** the SS income entry (`SSDataEntryView.swift` / income source model / `DataManager`), the withholding→dollars resolution, and the tax/safe-harbor consumer. Test: `RetireSmartIRATests/SSWithholdingRateTests.swift`.

**Change:** replace the free-form dollar "Annual Federal Withholding" for Social Security with a picker: **None / 7% / 10% / 12% / 22%** (the only IRS-legal SS rates). Store the rate; the withheld dollars fed to the tax/safe-harbor calc = rate × the SS benefit.

- [ ] **Step 1:** Read how SS withholding is stored today (`federalWithholding` on the income source) and consumed (safe-harbor / total withholding / quarterly). Design: add an SS-specific withholding rate (enum or Double in {0,0.07,0.10,0.12,0.22}); the resolved dollars = rate × gross SS benefit. Confirm the consumer still gets dollars.
- [ ] **Step 2:** Back-compat: existing profiles have a dollar SS withholding. On load, map it to the nearest legal rate (or default to None and surface a one-time "SS withholding is now a percentage" note). Do NOT lose/zero data silently — pick the sensible default and document it.
- [ ] **Step 3:** Failing tests: (a) picking 12% yields withheld dollars = 0.12 × SS benefit fed to the calc; (b) back-compat mapping from a legacy dollar value; (c) None = 0. RED → implement → GREEN.
- [ ] **Step 4:** UI: replace the dollar field with the picker (SS only), with a caption "Social Security withholding is set as a percentage (IRS Form W-4V: 7, 10, 12, or 22%)." Clean build.
- [ ] **Step 5:** Full suite; review any safe-harbor/quarterly rebaselines (withholding dollars may shift for profiles that had a legacy SS dollar value). Commit.

### Task 6 (#5b): Other income sources → $/% withholding toggle
**Files:** income source model + `IncomeSourcesView.swift` withholding entry. Test: `RetireSmartIRATests/IncomeWithholdingModeTests.swift`.

**Change:** for non-SS income (pension, IRA/401k distributions, etc.), let withholding be entered as either a dollar amount OR a percentage (toggle), defaulting to %. Resolve to dollars for the calc.

- [ ] **Step 1:** Add a per-source withholding MODE (`.dollars` / `.percent`) + value; resolved dollars = mode == .percent ? rate × source annual amount : value. Back-compat: existing dollar values default to `.dollars` mode (byte-identical behavior).
- [ ] **Step 2:** Failing tests: percent mode resolves rate × income; dollar mode unchanged (legacy byte-identical); the resolved dollars feed the same calc. RED → implement → GREEN.
- [ ] **Step 3:** UI: a $/% toggle on the withholding field per income source. No em dash. Clean build.
- [ ] **Step 4:** Full suite; confirm legacy dollar-mode profiles are byte-identical (no rebaselines except intentional). Commit.
- [ ] **NOTE:** if this task balloons (touches many income types / persistence migration), it can be deferred to a follow-up WITHOUT blocking 1-5a — flag and stop rather than rush the model.

### Task 7: Phase gate — full suite + both-platform build
- [ ] Full suite green (`-resultBundlePath` + xcresulttool). Review every rebaseline (only #5 should cause any; confirm correct). Clean build macOS + iOS. Commit any reviewed test updates.

## Self-Review
- Coverage: #1=T1, #3=T2, #2=T3, #4=T4, #5a=T5, #5b=T6, gate=T7. ✓
- #5 back-compat (legacy dollar withholding) explicitly handled in T5/T6 — no silent data loss.
- Engine dollar-semantics preserved; only the input model changes (T5/T6 resolve to dollars).
- iOS-vs-macOS keyboard gating called out (T1).
- Placeholder scan: SS-view internals (cell color, filter) are discovered during execution (grep) per the established plan style; production steps name files/symbols.
